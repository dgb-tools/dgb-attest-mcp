# Copy to .env. Never commit .env.
DGB_RPC_URL=http://127.0.0.1:14026
DGB_RPC_USER=your-rpc-user
DGB_RPC_PASSWORD=your-strong-rpc-password
DGB_WALLET=mcp-sandbox
# Attesting spends a fee, so testnet-only unless you deliberately opt in:
ATTEST_ALLOW_MAINNET=false
ATTEST_MAX_DAILY=50
